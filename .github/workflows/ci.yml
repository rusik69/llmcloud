name: CI/CD Pipeline

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

env:
  REGISTRY: ghcr.io

jobs:
  test:
    name: Test
    runs-on: self-hosted
    container:
      image: golang:1.25
      options: --user root
    steps:
      - name: Fix permissions
        run: |
          chmod -R 777 $GITHUB_WORKSPACE || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        run: |
          apt-get update && apt-get install -y curl
          curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
          apt-get install -y nodejs

      - name: Setup dependencies
        run: |
          go mod tidy
          make tools

      - name: Build frontend
        run: |
          cd web && npm install && npm run build

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Upload Coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./cover.out
          fail_ci_if_error: false

  build-operator:
    name: Build Operator Image
    runs-on: self-hosted
    needs: test
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Clean workspace
        run: |
          sudo rm -rf $GITHUB_WORKSPACE/* || true
          sudo rm -rf $GITHUB_WORKSPACE/.* || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push operator image
        run: |
          TAGS="${{ steps.meta.outputs.tags }}"
          FIRST_TAG=$(echo "$TAGS" | head -n1)
          echo "Building with primary tag: $FIRST_TAG"
          IMG="$FIRST_TAG" make docker-buildx
          # Create additional tags using buildx imagetools
          echo "$TAGS" | while read -r tag; do
            if [ -n "$tag" ] && [ "$tag" != "$FIRST_TAG" ]; then
              echo "Creating tag: $tag"
              docker buildx imagetools create --tag "$tag" "$FIRST_TAG"
            fi
          done

  deploy-and-e2e:
    name: Deploy and E2E Test
    runs-on: self-hosted
    needs: build-operator
    if: false  # Disabled until k3s cluster certificates are fixed
    steps:
      - name: Clean workspace
        run: |
          sudo rm -rf $GITHUB_WORKSPACE/* || true
          sudo rm -rf $GITHUB_WORKSPACE/.* || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Install tools
        run: make tools

      - name: Deploy operator to local cluster
        run: |
          # Use k3s kubeconfig with insecure-skip-tls-verify
          sudo chmod 644 /etc/rancher/k3s/k3s.yaml
          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml

          # Uninstall previous deployment if exists
          kubectl --insecure-skip-tls-verify=true delete namespace llmcloud-operator-system --wait=false || true
          make uninstall KUBECTL_FLAGS="--insecure-skip-tls-verify=true" || true

          # Install CRDs
          make install KUBECTL_FLAGS="--insecure-skip-tls-verify=true"

          # Deploy operator with latest image
          IMG=ghcr.io/${{ github.repository }}:latest make deploy KUBECTL_FLAGS="--insecure-skip-tls-verify=true"

          # Wait for deployment to be ready
          kubectl --insecure-skip-tls-verify=true wait --for=condition=available --timeout=300s \
            deployment/llmcloud-operator-controller-manager \
            -n llmcloud-operator-system

      - name: Run E2E tests
        env:
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
          USE_EXISTING_CLUSTER: "true"
          IMG: ghcr.io/${{ github.repository }}:latest
        run: |
          go test -v -tags=e2e ./test/e2e/... -timeout 30m

      - name: Collect logs on failure
        if: failure()
        env:
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
        run: |
          kubectl --insecure-skip-tls-verify=true get pods -n llmcloud-operator-system
          kubectl --insecure-skip-tls-verify=true describe pods -n llmcloud-operator-system
          kubectl --insecure-skip-tls-verify=true logs -n llmcloud-operator-system -l control-plane=controller-manager --tail=100

      - name: Cleanup
        if: always()
        env:
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
        run: |
          make undeploy KUBECTL_FLAGS="--insecure-skip-tls-verify=true" || true
          make uninstall KUBECTL_FLAGS="--insecure-skip-tls-verify=true" || true

