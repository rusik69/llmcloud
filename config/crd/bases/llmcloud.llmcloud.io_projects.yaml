---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: projects.llmcloud.llmcloud.io
spec:
  group: llmcloud.llmcloud.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.vmCount
      name: VMs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Project is the Schema for the projects API
          A Project represents a multi-tenant workspace with its own namespace and RBAC rules
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of Project
            properties:
              description:
                description: Description is a human-readable description of the project
                type: string
              members:
                description: Members is a list of project members with their roles
                items:
                  description: ProjectMember defines a member of the project
                  properties:
                    role:
                      default: viewer
                      description: Role is the role of the member (owner, admin, developer,
                        viewer)
                      enum:
                      - owner
                      - admin
                      - developer
                      - viewer
                      type: string
                    username:
                      description: Username is the Kubernetes user or service account
                        name
                      type: string
                  required:
                  - role
                  - username
                  type: object
                type: array
              resourceQuotas:
                description: ResourceQuotas defines resource limits for the project
                properties:
                  maxCPU:
                    description: MaxCPU is the maximum CPU allocation (e.g., "10")
                    type: string
                  maxLLMModels:
                    description: MaxLLMModels is the maximum number of LLM models
                      allowed
                    format: int32
                    type: integer
                  maxMemory:
                    description: MaxMemory is the maximum memory allocation (e.g.,
                      "20Gi")
                    type: string
                  maxVMs:
                    description: MaxVMs is the maximum number of VMs allowed
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: ProjectStatus defines the observed state of Project
            properties:
              conditions:
                description: Conditions represent the current state of the Project
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              llmModelCount:
                description: LLMModelCount is the current number of LLM models in
                  the project
                format: int32
                type: integer
              namespace:
                description: Namespace is the Kubernetes namespace created for this
                  project
                type: string
              phase:
                description: Phase represents the current phase of the project
                type: string
              serviceCount:
                description: ServiceCount is the current number of services in the
                  project
                format: int32
                type: integer
              vmCount:
                description: VMCount is the current number of VMs in the project
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
