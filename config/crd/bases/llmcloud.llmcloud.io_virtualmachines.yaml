---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: virtualmachines.llmcloud.llmcloud.io
spec:
  group: llmcloud.llmcloud.io
  names:
    kind: VirtualMachine
    listKind: VirtualMachineList
    plural: virtualmachines
    singular: virtualmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.node
      name: Node
      type: string
    - jsonPath: .status.ipAddress
      name: IP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VirtualMachine is the Schema for the virtualmachines API
          A VirtualMachine represents a KubeVirt virtual machine
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineSpec defines the desired state of VirtualMachine
            properties:
              cloudInit:
                description: CloudInit is the cloud-init user data
                type: string
              cpus:
                default: 1
                description: CPUs is the number of CPUs for the VM
                format: int32
                minimum: 1
                type: integer
              diskSize:
                default: 10Gi
                description: DiskSize is the size of the persistent disk (e.g., "10Gi")
                type: string
              memory:
                default: 1Gi
                description: Memory is the amount of memory for the VM (e.g., "2Gi")
                type: string
              os:
                description: OS is the operating system for the VM
                enum:
                - ubuntu
                - fedora
                - debian
                - centos
                - alpine
                - cirros
                - freebsd
                type: string
              osVersion:
                description: OSVersion is the version of the OS (optional, uses latest
                  if not specified)
                type: string
              runStrategy:
                default: Always
                description: RunStrategy defines the VM run strategy (Always, RerunOnFailure,
                  Manual, Halted)
                enum:
                - Always
                - RerunOnFailure
                - Manual
                - Halted
                type: string
              sshKeys:
                description: SSHKeys is a list of SSH public keys to inject
                items:
                  type: string
                type: array
              storageClass:
                description: StorageClass is the storage class for the VM disk
                type: string
            required:
            - os
            type: object
          status:
            description: VirtualMachineStatus defines the observed state of VirtualMachine
            properties:
              conditions:
                description: Conditions represent the current state of the VirtualMachine
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ipAddress:
                description: IPAddress is the IP address of the VM
                type: string
              node:
                description: Node is the node where the VM is running
                type: string
              phase:
                description: Phase is the current phase of the VM (Pending, Running,
                  Stopped, Failed)
                type: string
              ready:
                description: Ready indicates if the VM is ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
